# LLM provider. "dummy" lets the app start without any API keys.
ANTOSHKA_LLM_PROVIDER=dummy

# OpenAI credentials (only needed if ANTOSHKA_LLM_PROVIDER=openai)
OPENAI_API_KEY=put_your_key_here
# OPENAI_BASE_URL=https://api.openai.com/v1
